spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev                   #开发环�?
  application:
    name: service-config-dev
    
---
spring:
  profiles: test                  #测试环境
  application:
    name: service-config-test